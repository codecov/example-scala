package com.awesomeness

class Awesomeness {
  def square(x:Int): Int= {
    x*x
  }
  def multiple(x:Int,y:Int): Int= {
    x*y
  }
  def branch(x: Int): Unit = {
    if(x > 0) {
      ()
    }
  }
}
